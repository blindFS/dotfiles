#!/usr/bin/env bash
xbacklight -set 50
amixer set Master 20%
xscreensaver -nosplash &
pulseaudio --start

if [ -f "${HOME}/.Xresources" ]; then
    xrdb -load "${HOME}/.Xresources"
fi

if [ -f "${HOME}/.myxmodmap" ]; then
    xmodmap "${HOME}/.myxmodmap"
fi
